Ticked dial gauge; ticks sweep on staggered at mount. Use for utilization/load next to Gauge (solid-arc sibling).

\`\`\`jsx
<SegmentGauge value={64} label="CPU load" color="var(--accent-purple)" />
\`\`\`
